fn main() -> i32 {
  let arr: [i32; 5] = {1, 2, 3, 4, 5};
  let p: *mut i32 = arr;

  let sum: i32 = 0;
  sum = sum + p[0];
  sum = sum + p[1];
  sum = sum + p[2];
  sum = sum + p[3];
  sum = sum + p[4];

  return sum;
}
