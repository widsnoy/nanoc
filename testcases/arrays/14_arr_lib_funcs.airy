fn main() -> i32 {
  let arr: [i32; 10];
  let n: i32 = getarray(arr);
  let i: i32 = 0;
  while (i < 10) {
    if (i < n) {
      arr[i] = arr[i] + i;
    } else {
      arr[i] = arr[i - 1] + i;
    }
    i = i + 1;
  }
  putarray(n, arr);
  return 0;
}
