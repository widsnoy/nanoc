struct Point { x: i32, y: i32 }

let g_points: [const struct Point; 3] = {{1, 2}, {3, 4}, {5, 6}};

fn main() -> i32 {
  let sum: i32 = 0;
  let i: i32 = 0;
  while (i < 3) {
    sum = sum + g_points[i].x + g_points[i].y;
    i = i + 1;
  }
  return sum;
}
